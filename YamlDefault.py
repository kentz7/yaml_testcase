#encoding:utf-8

# HTTP 默认值
HTTP_DEFAULT = {
    "auth": (),
    "url": "",
    "method": "GET",
    "action": "/",
    "header": {
        "Content-Type": "application/json"
    }
}

# INT类型的默认值
VARIABLE_INT = [0, 1, 99]

# String类型的默认值
VARIABLE_STRING = ["", "NULL", "none", "TestName"]

# Bool类型的默认值
VARIABLE_BOOL = [False, True]
